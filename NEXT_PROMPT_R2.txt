1. zhttp 那个自动去重没真生效。我开了 EnableDedup 跑了 5 个并发 GET 同一个 URL，server 端被打了 5 次。
2. 配套的 dedup 单元测试 6/11 全挂：基本去重、带 body 去重、错误共享、并发合并、窗口过期复用这几个核心场景都不好使，hash 算得对但重复请求者没等第一个完成就自己重新跑了一遍下游。
3. zlog 结构化 JSON 那个测试文件本身编译就过不去，调用日志库 New 的方式跟实际签名都对不上，product 跟 test 之间接口完全错位，根本没法回归。
检查修复一下。