在 znet Web 框架的基础上新增限流中间件：支持基于令牌桶的全局限流和按路由分组的独立限流配额，超限请求响应 429 并在响应头返回 Retry-After；在 zlog 日志库的基础上新增结构化日志输出模式：支持 JSON 格式输出，字段包含时间戳、级别、调用方文件和行号，日志写入前允许通过钩子动态注入自定义字段，同时保持非结构化模式的性能不退化太多；在 zhttp 客户端的基础上新增自动去重能力：同一请求（方法和 URL 以及请求体摘要均相同的请求）在指定时间窗口内发起多次时，只发一次实际请求，后续等待者共享同一个响应，防止高频重复调用打垮下游。